[project]
name = "fast-api-mcp"
version = "0.1.0"
description = "Sequential Thinking MCP Server over FastAPI with Streamable HTTP"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.12",
    "mcp[cli]>=1.9.3",
    "python-dotenv>=1.1.0",
]

